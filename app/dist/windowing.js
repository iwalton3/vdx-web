import{reactive,flushSync,withoutTracking,createEffect}from'./framework.js';import{rafThrottle}from'./utils.js';function resolve(value,fallback){const v=typeof value==='function'?value():value;return (v===undefined||v===null||Number.isNaN(v))?fallback:v;}function findScrollableParent(el){let parent=el.parentElement;while (parent){const style=getComputedStyle(parent);const overflow=style.overflow+style.overflowY;if (overflow.includes('auto')||overflow.includes('scroll')){return parent;}parent=parent.parentElement;}return null;}export function createWindowing(host,options){const opts=options||{};if (!host||!opts.itemHeight||typeof opts.count!=='function'){throw new Error('[windowing] createWindowing(host, { itemHeight, count }) - host, itemHeight, and count are required');}const itemHeight=()=>resolve(opts.itemHeight,1);const buffer=()=>resolve(opts.buffer,10);const fallbackHeight=()=>resolve(opts.fallbackHeight,400);const overscan=()=>resolve(opts.overscan,0);function measureElement(){if (typeof opts.measureElement==='function'){return opts.measureElement()||host;}return opts.measureElement||host;}let initialEnd=20;try{initialEnd=Math.ceil(fallbackHeight()/itemHeight())+buffer();}catch{}const state=reactive({scrollTop:0,containerHeight:0,visibleStart:0,visibleEnd:initialEnd});let scrollTarget=null;let scrollHandler=null;let scrollMode=opts.scrollContainer||'self';let resizeObserver=null;let countEffect=null;let destroyed=false;function measureHeight(){if (scrollMode==='self'){return host.clientHeight||fallbackHeight();}if (scrollTarget===window){return window.innerHeight;}if (scrollTarget){return scrollTarget.clientHeight;}return fallbackHeight();}function measureScrollTop(){if (scrollMode==='self'){return host.scrollTop;}if (scrollTarget===window){const rect=measureElement().getBoundingClientRect();return Math.max(0,-rect.top);}if (scrollTarget){const parentRect=scrollTarget.getBoundingClientRect();const thisRect=measureElement().getBoundingClientRect();return Math.max(0,parentRect.top-thisRect.top);}return 0;}let lastScrollTop;let lastScrollTime;function updateVisibleRange(){if (destroyed) return;const rowH=itemHeight();const buf=buffer();const total=withoutTracking(()=>opts.count());let bufferAbove=buf;let bufferBelow=buf;const over=overscan();if (over>0){const now=performance.now();if (lastScrollTop!==undefined&&lastScrollTime!==undefined){const timeDelta=now-lastScrollTime;if (timeDelta>0&&timeDelta<200){const scrollDelta=state.scrollTop-lastScrollTop;if (Math.abs(scrollDelta/timeDelta)>0.5){if (scrollDelta>0) bufferBelow+=over;else bufferAbove+=over;}}}lastScrollTop=state.scrollTop;lastScrollTime=now;}const first=Math.floor(state.scrollTop/rowH);const visibleCount=Math.ceil((state.containerHeight||fallbackHeight())/rowH);let newStart=Math.max(0,first-bufferAbove);let newEnd=Math.min(total,first+visibleCount+bufferBelow);const loaded=typeof opts.loadedCount==='function'?withoutTracking(()=>opts.loadedCount()):total;if (loaded>0&&loaded<total){newStart=Math.min(newStart,loaded-1);newEnd=Math.min(newEnd,loaded);newEnd=Math.max(newEnd,newStart+1);}const renderCount=newEnd-newStart;newStart=Math.min(newStart,Math.max(0,loaded-renderCount));if (state.visibleStart!==newStart||state.visibleEnd!==newEnd){flushSync(()=>{state.visibleStart=newStart;state.visibleEnd=newEnd;});if (typeof opts.onRange==='function'){opts.onRange(newStart,newEnd);}}}function handleScroll(){if (destroyed) return;state.scrollTop=measureScrollTop();updateVisibleRange();}function updateDimensions(){if (destroyed) return;const newHeight=measureHeight();if (state.containerHeight!==newHeight){state.containerHeight=newHeight;updateVisibleRange();}}function cleanupScrollTracking(){if (scrollHandler){if (scrollTarget===window){window.removeEventListener('scroll',scrollHandler,true);}else if (scrollTarget){scrollTarget.removeEventListener('scroll',scrollHandler);}scrollHandler=null;scrollTarget=null;}}function setupScrollTracking(){cleanupScrollTracking();const listenerOptions={passive:true};scrollHandler=rafThrottle(handleScroll);if (scrollMode==='self'){host.addEventListener('scroll',scrollHandler,listenerOptions);scrollTarget=host;}else if (scrollMode==='window'){window.addEventListener('scroll',scrollHandler,{capture:true,passive:true});scrollTarget=window;}else if (scrollMode==='parent'){scrollTarget=findScrollableParent(host);if (scrollTarget){scrollTarget.addEventListener('scroll',scrollHandler,listenerOptions);}else{window.addEventListener('scroll',scrollHandler,{capture:true,passive:true});scrollTarget=window;}}else if (typeof scrollMode==='string'){scrollTarget=document.querySelector(scrollMode);if (scrollTarget){scrollTarget.addEventListener('scroll',scrollHandler,listenerOptions);}else{console.warn(`[windowing] scrollContainer "${scrollMode}" not found, falling back to self`);host.addEventListener('scroll',scrollHandler,listenerOptions);scrollTarget=host;}}else if (scrollMode instanceof Element){scrollTarget=scrollMode;scrollTarget.addEventListener('scroll',scrollHandler,listenerOptions);}applyAnchorOptOut();}function applyAnchorOptOut(){try{const content=measureElement();if (content&&content.style) content.style.overflowAnchor='none';}catch{}if (scrollTarget&&scrollTarget!==window&&scrollTarget.style){scrollTarget.style.overflowAnchor='none';}}function attach(){setupScrollTracking();if (!resizeObserver&&typeof ResizeObserver!=='undefined'){resizeObserver=new ResizeObserver(()=>updateDimensions());resizeObserver.observe(host);}remeasureScroll();updateDimensions();updateVisibleRange();}function detach(){cleanupScrollTracking();if (resizeObserver){resizeObserver.disconnect();resizeObserver=null;}}function remeasureScroll(){if (scrollMode==='self'||scrollTarget){state.scrollTop=measureScrollTop();}}queueMicrotask(()=>{if (destroyed) return;attach();withoutTracking(()=>{countEffect=createEffect(()=>{opts.count();if (!destroyed){updateVisibleRange();}});});});const controller={get visibleStart(){return state.visibleStart;},get visibleEnd(){return state.visibleEnd;},get offsetY(){return state.visibleStart*itemHeight();},get totalHeight(){return opts.count()*itemHeight();},get scrollTop(){return state.scrollTop;},get containerHeight(){return state.containerHeight;},refresh(){remeasureScroll();updateDimensions();updateVisibleRange();applyAnchorOptOut();},scrollToIndex(index){flushSync(()=>{});const target=index*itemHeight();if (scrollMode==='self'){host.scrollTop=target;}else if (scrollTarget===window){const rect=host.getBoundingClientRect();window.scrollTo({top:window.scrollY+rect.top+target,behavior:'smooth'});}else if (scrollTarget){const thisRect=host.getBoundingClientRect();const parentRect=scrollTarget.getBoundingClientRect();const offset=thisRect.top-parentRect.top+scrollTarget.scrollTop;scrollTarget.scrollTop=offset+target;}},scrollToTop(){controller.scrollToIndex(0);},scrollToBottom(){const total=withoutTracking(()=>opts.count());controller.scrollToIndex(Math.max(0,total-1));},setScrollContainer(mode){scrollMode=mode;setupScrollTracking();remeasureScroll();updateDimensions();updateVisibleRange();},attach(){if (!destroyed) attach();},detach,destroy(){destroyed=true;detach();if (countEffect){countEffect.dispose();countEffect=null;}}};return controller;}
//# sourceMappingURL=windowing.js.map
