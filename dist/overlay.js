/*! VDX overlay v1.1.0 | MIT | https://vanilladx.dev | https://github.com/iwalton3/vdx-web */
import{rafThrottle,clamp}from'./utils.js';const POPOVER_SUPPORTED=typeof HTMLElement!=='undefined'&&typeof HTMLElement.prototype.showPopover==='function'&&HTMLElement.prototype.hasOwnProperty('popover');function resolveEl(value){const el=typeof value==='function'?value():value;return el||null;}
function anchorRect(ref){if (typeof ref.getBoundingClientRect==='function') return ref.getBoundingClientRect();const x=ref.x||0,y=ref.y||0,w=ref.width||0,h=ref.height||0;return{left:x,top:y,right:x+w,bottom:y+h,width:w,height:h};}
function parsePlacement(placement){const[rawSide,rawAlign='start']=String(placement||'bottom-start').split('-');const side=['top','bottom','left','right'].includes(rawSide)?rawSide:'bottom';const align=rawAlign==='end'?'end':rawAlign==='center'?'center':'start';return{side,align};}
export function createAnchoredOverlay(host,opts){const{offset=4,viewportPadding=8,matchAnchorWidth=false,closeOnScroll=false,onDismiss=()=>{},}=opts;let shown=false;let destroyed=false;let panelEl=null;let resolvedSide=null;let resolvedAlign=null;function reposition(){if (!shown) return;const anchor=resolveEl(opts.anchor);const panel=panelEl||resolveEl(opts.panel);if (!anchor||!panel) return;const placement=typeof opts.placement==='function'?opts.placement():opts.placement;const{side,align}=parsePlacement(placement);const vw=window.innerWidth;const vh=window.innerHeight;const pad=viewportPadding;const a=anchorRect(anchor);if (matchAnchorWidth) panel.style.width=`${a.width}px`;panel.style.maxHeight='';const p=panel.getBoundingClientRect();const panelW=p.width;let panelH=p.height;let top,left,maxHeight=null,resolved=side;if (side==='top'||side==='bottom'){const below=vh-pad-a.bottom;const above=a.top-pad;const placeBelow=side==='bottom'?(panelH+offset<=below||below>=above):!(panelH+offset<=above||above>=below);resolved=placeBelow?'bottom':'top';const avail=Math.max(0,(placeBelow?below:above)-offset);const h=Math.min(panelH,avail);if (h<panelH) maxHeight=h;top=placeBelow?a.bottom+offset:a.top-offset-h;top=clamp(top,pad,Math.max(pad,vh-pad-h));left=align==='end'?a.right-panelW:align==='center'?a.left+(a.width-panelW)/2:a.left;left=clamp(left,pad,Math.max(pad,vw-pad-panelW));}else{const right=vw-pad-a.right;const leftSpace=a.left-pad;const placeRight=side==='right'?(panelW+offset<=right||right>=leftSpace):!(panelW+offset<=leftSpace||leftSpace>=right);resolved=placeRight?'right':'left';left=placeRight?a.right+offset:a.left-offset-panelW;left=clamp(left,pad,Math.max(pad,vw-pad-panelW));const availV=vh-2*pad;if (panelH>availV){maxHeight=availV;panelH=availV;}
top=align==='end'?a.bottom-panelH:align==='center'?a.top+(a.height-panelH)/2:a.top;top=clamp(top,pad,Math.max(pad,vh-pad-panelH));}panel.style.position='fixed';panel.style.margin='0';panel.style.inset='auto';panel.style.top=`${Math.round(top)}px`;panel.style.left=`${Math.round(left)}px`;panel.style.maxHeight=maxHeight!=null?`${Math.round(maxHeight)}px`:'';resolvedSide=resolved;resolvedAlign=align;if (typeof opts.onReposition==='function'){opts.onReposition({side:resolved,align});}}
const onScroll=closeOnScroll?()=>{if (shown) onDismiss('scroll');}:rafThrottle(reposition);const onResize=rafThrottle(reposition);function onPointerDown(e){if (!shown) return;const anchor=resolveEl(opts.anchor);const target=e.target;if (panelEl&&panelEl.contains(target)) return;if (anchor&&typeof anchor.contains==='function'&&anchor.contains(target)) return;onDismiss('outside');}
function onKeyDown(e){if (shown&&e.key==='Escape'){e.stopPropagation();onDismiss('escape');}}
function attachListeners(){window.addEventListener('scroll',onScroll,true);window.addEventListener('resize',onResize);document.addEventListener('pointerdown',onPointerDown,true);document.addEventListener('keydown',onKeyDown,true);}
function detachListeners(){window.removeEventListener('scroll',onScroll,true);window.removeEventListener('resize',onResize);document.removeEventListener('pointerdown',onPointerDown,true);document.removeEventListener('keydown',onKeyDown,true);}
return{get isOpen(){return shown;},get placement(){return resolvedSide?`${resolvedSide}-${resolvedAlign}`:null;},open(){if (destroyed||shown) return;const panel=resolveEl(opts.panel);if (!panel) return;panelEl=panel;if (POPOVER_SUPPORTED){if (!panel.hasAttribute('popover')) panel.setAttribute('popover','manual');if (!panel.matches(':popover-open')){try{panel.showPopover();}catch (_){}}}
shown=true;reposition();attachListeners();},close(){if (!shown) return;shown=false;detachListeners();const panel=panelEl;panelEl=null;if (POPOVER_SUPPORTED&&panel&&panel.matches(':popover-open')){try{panel.hidePopover();}catch (_){}}},reposition,destroy(){destroyed=true;this.close();},};}
//# sourceMappingURL=overlay.js.map
